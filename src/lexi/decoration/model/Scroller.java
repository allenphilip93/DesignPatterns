package lexi.decoration.model;

public class Scroller extends MonoGlyph {

    public void draw(Window window) {
        super.draw(window);
        drawScroller(window);
    }

    private void drawScroller(Window window) {
        // insert logic for drawing scroller for the enclosing monoglyph
    }
}
